import React from 'react';
import ReactDOM from 'react-dom/client';
import CECreditCalculator from './CECreditCalculator';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <CECreditCalculator />
    </React.StrictMode>
);
